"""Cockpit local — couche présentation (jetable) au-dessus de la façade Sdlc.

`data.py` est pur (testable) ; `server.py` l'expose en REST (FastAPI, optionnel).
Mapping factory : REST → mission-control ; cette page → cockpit/ React.
"""
